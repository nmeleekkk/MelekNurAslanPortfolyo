@model MelekNurAslanPortfolyo.Models.Certificate
@{
    ViewData["Title"] = "Sertifika Güncelle";
    Layout = "_AdminLayout";
}

<div class="card mt-3">
    <div class="card-header">
        <h3 class="card-title">Sertifika Güncelle</h3>
    </div>
    <div class="card-body">
        <form method="post" enctype="multipart/form-data">

            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="ImageUrl" />

            <div class="form-group mb-3">
                <label>Sertifika Adı</label>
                <input type="text" asp-for="Title" class="form-control" required />
            </div>

            <div class="form-group mb-3">
                <label>Sertifika Açıklaması</label>
                <textarea asp-for="Description" class="form-control" rows="4" required></textarea>
            </div>

            <div class="form-group mb-3">
                <label>Sertifika Görseli</label>
                <input type="file" name="imageFile" class="form-control" />
            </div>

            <button class="btn btn-warning">Güncelle</button>
            <a href="/Certificate/Index" class="btn btn-secondary">İptal</a>
        </form>
    </div>
</div>